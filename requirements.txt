lxml==6.1.0
supabase>=2.0.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
requests==2.33.1
cssselect==1.4.0
fastapi==0.136.0
uvicorn[standard]==0.45.0
pydantic==2.13.3
starlette==1.0.0
anyio==4.13.0
h11==0.16.0
httpx==0.28.1
pytest==9.0.3
python-dotenv==1.2.2
sqlalchemy==2.0.49
psycopg2-binary==2.9.12
gunicorn==25.3.0
# GS1 product imagery: the API returns base64 originals at up to 4800x4800
# (~2.8 MB avg). Pillow resizes them to 800px/JPEG-80 before they touch disk —
# storing raw would need ~32 GB, more than the whole 30 GB volume.
Pillow==12.3.0
